---
title: "ProgVLA: Progress-Aware Robot Manipulation Skill Learning"
title_zh: ProgVLA：进度感知的机器人操作技能学习
authors: "Seungsu Kim, Jinyoung Choi, Seungmin Baek, Jean-Michel Renders"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28231v1"
tags: ["query:robotics"]
score: 9.0
evidence: 具有进度感知的机器人操作技能学习
tldr: 针对资源受限场景下机器人操作处理长多模态序列的挑战，提出ProgVLA紧凑型VLA模型。采用两阶段Perceiver重采样编码器压缩视觉、语言和本体感受流为固定上下文令牌，并引入进度头通过离线强化学习估计任务剩余进度，实现优势加权流匹配模仿学习。在标准基准上，0.1B参数模型性能媲美甚至超越大型预训练模型，尤其在长时域和多物体任务中提升显著，并在真实玩具厨房环境中验证了有效性。该工作展示了紧凑模型通过显式进度感知和高效压缩可胜任复杂长时域操作。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28231v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28231v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 791, \"height\": 503, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28231v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1269, \"height\": 869, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1168, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1485, \"height\": 1023, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1087, \"height\": 671, \"label\": \"Table\"}]"
motivation: 解决计算和内存限制下机器人操作对长多模态序列高效处理的需求，同时保持任务进度感知以提升长时域性能。
method: 设计两阶段Perceiver重采样编码器压缩多模态输入，并集成进度头通过离线强化学习训练残差进度估计，实现优势加权流匹配。
result: 0.1B参数ProgVLA在基准上与大型模型竞争，长时域和多物体任务成功率超越，真实环境验证有效。
conclusion: 紧凑模型结合显式进度感知和高效序列压缩，为资源受限机器人学习提供实用方案。
---

## 摘要
我们提出了ProgVLA，一种紧凑的视觉-语言-动作（VLA）模型，旨在在严格的计算和内存预算下实现可靠的机器人操作。该模型特别关注通过维护长时间跨度的任务进度的显式表示，来高效处理长多模态序列。为此，ProgVLA集成了两个关键组件。首先，具有两阶段感知器重采样方案的多模态编码器将可变长度的视觉、语言和本体感受流压缩成一组固定的控制准备上下文令牌，在保持跨模态根基的同时显著减少序列长度。其次，一组辅助的进度头通过离线强化学习（RL）目标进行训练，以联合学习对归一化剩余时间目标的评价器。这为策略提供了任务进度的内部估计，并实现了优势和成功加权的流匹配模仿学习。在两个公认的多任务机器人操作基准上，一个0.1B参数的ProgVLA模型达到了与显著更大的预训练基线相当的成功率，并且在长时域和更困难的任务层级上超过了它们。消融实验表明，学习的上下文重采样器和任务自适应视觉微调是最大的单一贡献者，而进度感知训练提供了集中在长时域和多对象任务上的一致额外增益。我们还在现实世界的玩具厨房环境中进一步验证了该方法。

## Abstract
We present ProgVLA, a compact vision-language-action (VLA) model designed for reliable robot manipulation under tight compute and memory budgets. The model specifically focuses on efficiently processing long multi-modal sequences by maintaining an explicit representation of task progress over extended horizons. To this end, ProgVLA integrates two key components. First, a multi-modal encoder with a two-stage Perceiver resampling scheme compresses variable-length visual, language, and proprioceptive streams into a fixed set of control-ready context tokens, substantially reducing sequence length while preserving cross-modal grounding. Second, an auxiliary set of progress heads is trained with offline reinforcement learning (RL) objectives to jointly learn critics over normalized remaining-horizon targets. This provides the policy with an internal estimate of task progress and enables advantage- and success-weighted flow-matching imitation learning. On two well-established multi-task robot manipulation benchmarks, a 0.1B-parameter ProgVLA model reaches success rates that are competitive with, and on long-horizon and harder task tiers exceed, substantially larger pretrained baselines. Ablations indicate that the learned context resampler and task-adaptive visual fine-tuning are the largest single contributors, while progress-aware training provides a consistent additional gain that is concentrated on long-horizon and multi-object tasks. We further validate the approach in real-world toy-kitchen environments.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：现有大型视觉-语言-动作（VLA）模型（如OpenVLA、π₀）参数庞大（数B至7B+），依赖大规模跨本体机器人预训练，部署门槛高（专用硬件、高昂算力）。小型VLA（如SmolVLA）虽降低了参数规模，但在长时域任务上表现不佳。论文旨在回答：**能否以单一紧凑模型（0.1B参数），仅使用目标基准的演示数据（无跨本体预训练），在长时域、多物体复杂操作任务中达到甚至超越大型预训练模型？**
- **整体含义**：ProgVLA通过三项互补设计——高效跨模态特征压缩、强通用视觉先验（而非机器人数据预训练）、显式任务进度感知——证明了紧凑VLA在资源受限场景下实现可靠长时域操控的可行性，为机器人学习民主化提供了实用路径。

### 2. 方法论：核心思想、关键技术细节
- **核心思想**：设计一种参数仅0.1B的紧凑VLA，通过两阶段Perceiver重采样压缩多模态输入为少量上下文令牌，并引入辅助进度头（progress heads）联合离线强化学习目标，使策略内部估计任务进度，从而调整模仿学习权重。
- **关键技术细节**：
    1. **两阶段Perceiver重采样编码器**：
        - 第一阶段：对每种模态（视觉、语言、本体感受）分别用Perceiver Resampler将可变长度特征压缩为固定数量令牌（视觉每图像8个，语言16个，本体1个）。
        - 第二阶段：将各模态令牌拼接后经过共享Transformer进行跨模态融合，再经过后融合Perceiver Resampler蒸馏为4个紧凑的“上下文令牌”cₜ。
    2. **进度头（Progress Heads）**：
        - 定义形状化进度目标 rₜ = max(0, 1 - (T - t)/T_M)，其中T为轨迹长度，T_M为固定上限（500）。
        - 两个辅助头：状态-动作评价器 ˆQ(cₜ, aₜ)（回归rₜ， Huber损失）和状态价值 ˆV(cₜ)（IQL式期望回归，ρ=0.8），以及成功分类头 ˆS(cₜ)（二分类，阈值r_succ≈0.966）。
        - 优势 Aₜ = ˆQ - ˆV（分离计算图），转化为权重 w_A,t = min(exp(Aₜ/β), C)；成功概率权重 w_S,t = 0.5+0.5σ(ˆS)。
    3. **流匹配动作专家**：采用SmolVLA式流匹配模型，预测动作块（长度16），执行前8步，重规划。训练时使用优势权重与成功权重乘积加权流匹配损失，同时辅助损失λ_V L_V + λ_Q L_Q + λ_S L_S。
- **公式/算法流程**（文字说明）：整体损失包含加权流匹配损失（式1）加上三个辅助损失（式3-6），通过离线RL启发式设计（但无策略梯度，仅用作样本重加权）。核心创新在于进度头与策略共享上下文表示，使策略内部感知剩余进度。

### 3. 实验设计：数据集、场景与对比方法
- **模拟基准**：
    - **LIBERO**：40个任务（分为Spatial, Object, Goal, Long四类），每任务50条演示，共2000条。官方评估协议，每任务50次随机初始状态评价。
    - **Meta-World MT50**：LeRobot数据集（49个任务，按Easy/Medium/Hard/Very Hard分组），每任务50条演示。与[21]相同评估协议。
- **真实世界**：6-DOF PiPER机械臂，10个任务（玩具厨房+白色桌面场景），每任务50条遥操作演示，100次测试（每指令10次）。
- **对比方法**：OpenVLA (7B)、π₀ (3.3B及Paligemma-3B)、Octo (0.09B)、SmolVLA (0.24B & 2.25B)。基线数值来自[10,21]（未在原协议下重跑，存在演示数量、图像分辨率等差异）。
- **基准的公平性说明**：ProgVLA未使用大规模机器人预训练，仅使用目标基准演示；而对比基线依赖大规模跨本体预训练。此外，LIBERO演示数ProgVLA更多（2000 vs ~1693），但图像分辨率较低；Meta-World基线未在同一过滤条件下。论文明确这些差异不会改变定性结论。

### 4. 资源与算力
- **训练硬件**：单张NVIDIA H100 GPU。
- **训练时长**：
    - LIBERO模型：25小时
    - Meta-World模型：32小时
    - 真实世界模型：约8.5小时
- **参数规模**：全模型109M参数（含两个图像输入），其中74M可训练；Meta-World版100M/65M可训练。未提供推理延迟或FLOPs详细分析，仅提及在便携笔记本（Intel Ultra 7 CPU + RTX 3500 GPU）实时运行。

### 5. 实验数量与充分性
- **实验数量**：
    - LIBERO与Meta-World各训一个模型，报告50次/任务的成功率平均值。
    - 消融实验4个变体（去进度目标、去上下文重采样器、换DINOv3骨干、冻结DUNE），均在LIBERO上执行。
    - 真实世界10个任务×10次=100次测试。
- **充分性与客观性**：
    - **优势**：覆盖两个标准模拟基准（40+49任务）和真实场景，消融完善（四大变体），各组件贡献量化清晰。
    - **不足**：
        - 所有结果仅来自**单次训练种子**，论文提及run-to-run方差约±1-2个百分点，但未提供多种子统计。
        - 基线对比非严格复现，存在协议差异（演示数、图像分辨率、评估次数），可能影响公平性。
        - 未对进度头进行单独验证（如校准曲线、单调性分析）。
        - 超参数（T_M, ρ, β, C）未做敏感性扫描。

### 6. 主要结论与发现
- **性能超越**：0.1B ProgVLA在LIBERO总体成功率91.1%，长时域任务88.6%，超过OpenVLA (76.5%/53.7%)、SmolVLA-2.25B (88.75%/77%)等；在Meta-World更难层级（Medium/Very Hard）超越SmolVLA-2.25B 15.6-20.9个百分点。
- **消融关键因素**：后融合上下文重采样器贡献最大（移除后长时域成功率从88.6%骤降至51.2%）；视觉骨干微调次之（冻洁DUNE导致平均下降13.5%）；进度头提供小而一致的增益（集中在Object和Long类别，约2-5个百分点）。
- **真实结果**：10任务平均成功率68%，主要失败模式为场景遮挡（10/32）、夹爪超时（8/32）、抓错物体（4/32）。

### 7. 优点
- **方法创新**：
    - 两阶段Perceiver重采样首次在紧凑VLA中联合处理多模态，显著压缩序列长度（从数百令牌降至4个），同时保持跨模态对齐。
    - 进度头与策略共享表示，通过离线RL启发式加权增强长时域学习，无需额外标注或在线交互。
    - 无机器人预训练依赖，仅使用公开视觉骨干（DUNE）和冻结T5，降低门槛。
- **实验设计**：
    - 消融实验四大变体设计全面，清晰量化每个组件的贡献。
    - 真实世界验证，包含10个复杂任务和详细失败分析。

### 8. 不足与局限
- **实验覆盖不足**：
    - 仅使用单一种子训练，缺少多次运行统计。
    - 基线对比非严格复现（演示数、分辨率、评估次数差异），需谨慎解读绝对数值。
    - 未验证进度头的内部校准和泛化能力（如对失败轨迹的区分）。
    - 真实世界评估为同分布，未测试跨环境/跨平台泛化。
    - 无推理延迟、FLOPs等效率指标。
- **方法局限**：
    - 进度目标纯粹基于时间（剩余步骤），而非语义子目标，无法区分不同难度阶段。
    - 仅使用成功演示训练，无法从失败中学习；优势信号弱识别（action-granularity限制）。
    - 未进行超参数敏感性分析（T_M, ρ, β, C）。
    - 进度头贡献较小（平均2.3%），且只在长时域任务上明显。
- **偏差与安全性**：继承演示数据中的偏倚（如偏好抓取方式），部署需人类监督和硬件安全措施。

（完）
